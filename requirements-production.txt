# Runtime dependencies for scheduled text delivery.  Keep renderer-only
# Playwright/Pillow packages in requirements.txt for photo smoke tests, not
# in the production notification image.
requests>=2.32,<3
python-dotenv>=1.0,<2
yfinance>=0.2
pandas-market-calendars>=4.4
beautifulsoup4>=4.12,<5
lxml>=5.0,<7
html5lib>=1.1,<2
httpx>=0.27,<1
jsonschema>=4.23,<5
