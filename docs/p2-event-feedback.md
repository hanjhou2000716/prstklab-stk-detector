# P2-06 事件回饋資料集

`src/event_feedback.py` 提供匿名、可審核的事件回饋契約。回饋只保存事件鍵、
標籤、事件類型、來源層級與時間，不保存 Telegram 使用者、聊天 ID 或其他
個資。標籤包括正確、不相關、重複、方向錯誤、來源不足、太晚通知與不需要通知。

統計會輸出 precision、可選的 recall、誤報率、提醒實用度與平均送達延遲，並
固定標示 `policy_update_allowed=false`。未經人工審核的回饋不能直接改變警報
門檻；需先完成人工抽樣、版本化審查與回歸測試。
