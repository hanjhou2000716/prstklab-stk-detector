# Mini App 事件證據原因

事件詳情現在直接顯示 `evidence_state` 與 `evidence_reason`。探索或單一來源事件
會顯示「等待第二來源」；已完成來源核對但尚未取得相關市場反應時顯示「等待市場
同步」。這些文字與事件帳本、Telegram 的核對狀態一致，避免只看到新聞卻不知道
為何沒有升級或推播。

前端只讀取目前 release 的事件資料；缺欄位時保持舊版交叉核對顯示，確保向後相容。
