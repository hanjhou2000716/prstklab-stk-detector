# P1 Creator source health

Creator providers are optional enrichment sources and are appended to the same source-health contract when a producer supplies `creator_sources`. `no_event` means the provider completed a scan without new content; `failed`, `stale`, or `partial` remain explicit runtime gaps. Missing Creator content never downgrades the core market release or becomes event evidence.
