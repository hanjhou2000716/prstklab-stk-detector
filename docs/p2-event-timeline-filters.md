# P2 事件時間軸篩選

`src.event_timeline.build_timeline` 以穩定的 `event_cluster_key` 保留同一事件的版本連續性，並提供市場、分類、交叉核對狀態、風險層級與來源層級的精確篩選。

篩選不會改寫事件內容，也不會將待核對事件升級；R2／R3 的官方核對與市場同步條件仍由外部事件風險引擎決定。缺少欄位時不會猜測，該列只會在未指定該篩選時顯示。
