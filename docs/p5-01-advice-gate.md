# P5-01～P5-03 情境式建議閘門

只有資料品質、新鮮度、必要來源核對、正式回測、候選完整度與 policy 全部通過，才輸出中立的「觀察」情境。否則固定回覆「目前資料不足，僅能列為觀察候選，暫不提供操作判斷。」每個允許輸出都需包含證據、觸發、失效條件、反方論點、風險與替代情境，禁止保證式買賣語言。

驗證：`pytest -q tests/test_advice_gate.py`。

回滾：撤回本 PR 即可關閉建議 formatter，不影響研究資料。
