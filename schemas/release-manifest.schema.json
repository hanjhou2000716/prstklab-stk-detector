{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://prstk.example/schemas/release-manifest.schema.json",
  "type": "object",
  "required": [
    "release_id", "created_at", "market_snapshot_id", "research_snapshot_id",
    "event_snapshot_id", "policy_version", "schema_versions", "artifact_paths", "artifact_hashes", "status"
  ],
  "properties": {
    "release_id": {"type": "string", "minLength": 8},
    "created_at": {"type": "string", "format": "date-time"},
    "market_snapshot_id": {"type": "string", "minLength": 8},
    "research_snapshot_id": {"type": "string", "minLength": 8},
    "event_snapshot_id": {"type": "string", "minLength": 8},
    "policy_version": {"type": "string", "minLength": 1},
    "schema_versions": {"type": "object"},
    "artifact_paths": {
      "type": "object",
      "required": ["market.json", "research-report.json", "event-ledger.json"],
      "properties": {
        "market.json": {"type": "string", "minLength": 1},
        "research-report.json": {"type": "string", "minLength": 1},
        "event-ledger.json": {"type": "string", "minLength": 1}
      }
    },
    "artifact_hashes": {
      "type": "object",
      "required": ["market.json", "research-report.json", "event-ledger.json"],
      "properties": {
        "market.json": {"type": "string", "pattern": "^[a-fA-F0-9]{64}$"},
        "research-report.json": {"type": "string", "pattern": "^[a-fA-F0-9]{64}$"},
        "event-ledger.json": {"type": "string", "pattern": "^[a-fA-F0-9]{64}$"}
      }
    },
    "research_freshness": {"enum": ["fresh", "stale_fallback", "unverified", "unknown"]},
    "research_fallback_used": {"type": "boolean"},
    "research_fallback_reason": {"type": ["string", "null"]},
    "backtest_release": {"type": ["string", "null"]},
    "backtest_publication_state": {"enum": ["ready", "blocked", "unavailable"]},
    "strategy_registry": {"type": "array", "items": {"type": "object"}},
    "creator_release_id": {"type": ["string", "null"]},
    "creator_status": {"enum": ["ready", "unavailable", "not_available"]},
    "creator_validation_errors": {"type": "array", "items": {"type": "string"}},
    "creator_artifact_hash": {"type": ["string", "null"], "pattern": "^[a-fA-F0-9]{64}$"},
    "status": {"enum": ["ready", "rolled_back", "invalid"]}
  },
  "$defs": {
    "strategy_release": {
      "type": "object",
      "required": ["strategy_id", "strategy_version", "parameter_hash", "universe_version", "data_version", "code_commit", "backtest_release"],
      "properties": {
        "strategy_id": {"type": "string", "minLength": 1},
        "strategy_version": {"type": "string", "minLength": 1},
        "parameter_hash": {"type": "string", "minLength": 8},
        "universe_version": {"type": "string", "minLength": 1},
        "data_version": {"type": "string", "minLength": 1},
        "code_commit": {"type": "string", "minLength": 7},
        "backtest_release": {"type": "string", "minLength": 8}
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
